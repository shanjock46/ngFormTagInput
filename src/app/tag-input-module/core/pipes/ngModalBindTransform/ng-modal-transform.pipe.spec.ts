import {NgModalTransformPipe} from './ng-modal-transform.pipe';

describe('NgModalBindTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new NgModalTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
