import {NgBindTransformPipe} from './ng-bind-transform.pipe';

describe('NgModalBindTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new NgBindTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
